# JavaSCript

>날짜: 2026.09.21

----

## 자바스크립트
- 인터프리터 기반
- 자바스크립트 해석엔진
    - 웹브라우저(크롬)- V8
    - Node.js - V8 (런타임)
        - NVM(Node Version Manger)
    
**node**
- 자바스크립트 코드를 실행하는 rumtime

**npm : Node Package Manger.**
- 외부 패키지를 설치할떄 사용하는 명령어

**node.js 프로젝트 생성, 실행**

**npx: node Package Excutor**


## JavaScript 기초 문법
```JavaScript
var a =100;
```
- 번역과정 -> window 객체의 하위 속성으로 추가(웹 기준)/ global 객체의 하위 속성으로 추가(Node.js)

**호이스팅**
- 정의한 변수, 함수가 최상위 객체에 할당
    - 함수 정의 위치에 상관없이 호출 가능

### var의 3가지 결합
- 호이스팅 문제
- 변수의 유효범위(scope)
- 선언자(var)을 생략하고 변수를 지역변수로 정의하면 -> 전역변수가 된다.

**2015(EcmaScript 6)**

let : 값 변경 가능

const: 값 변경 불가능 (상수)
- 유효범위  block level
- 호이스팅 해결
- 중복 정의 문제 해결
- **자바스크립트에서는 변수를 선언할 떄 const로 선언하고 변경이 필요한 경우에만 let으로 변경** 

#### 자바스크립트의 주요 데이터 타입
- Number (숫자형) : 정수와 실수를 따로 구분하지 않고 모든 숫자 형식
- String (문자열) : '', ""로 감싼 텍스트
- Boolean (불리언) : ture, false
- null : 
- undefined: 정의되지 안흥ㅁ, 변수를 선언후 값을 대입하지 않으면 기본값

1. 인터프리터 기반 unction add (...) 
- 함수 객체(Function)
- EC(Excution Context) 객체 : 파이썬의 PyFrameObject와 유사
2. 연산자 오버로드 (+, -, /, *, ...)
- 연산자의 기능을 새롭게 정의
- 파이썬과 다르게 다른자료형과 결합하면 문자열로 값 도출


### 탬플릿 리터럴
