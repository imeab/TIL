
## Python 기초
> 날짜: 2026.08.06

----

가상환경이 필요 구버전과 신버전 둘다 필요함.

- 구축 방법
    1. python -m venv _____     (느리다, 의존성 문제)
    2. 아나콘다 (느리다, 의존성 문제)
    3. UV  (파워셀로 다운)
    - pwershell에서 설치 (powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex")
    - rust 기반 빠르다
    - 프로젝트 단위 관리
    - 의존성 궁합 잘 맞음
- 버전 표기 방식
    - uv    0(major버전). 12(Minor) , 2 (patch)
    - Major (하위 버전과 호환 x)
    - Minor(하위버전 호환 o , 기능 추가 작은 변경)
    - Patch (오류 수정, 코드 개선 오름)

### UV

- uv 프로젝트 생성 + 가상환경
    - uv init —python  버전!
    
    -uv init 파일 —python 버전 (생성과 파이선
    
    - .venv
    - 파이썬 활성화(.venv\Scripts\activate)
    - 비활성화 (.venv\Scripts\deactivate.bat)
    - uv_sync (동기화) 가상환경 복구?
    - 파이썬 변경 (uv python pin 3.12)
    - .py (파이썬이 인식하는
- 확장 기능 추가(vscode
    - python 설치
    - Jupyter 설치

## Python 문법

변수와 동적 타이핑

메모리: 값들을 기록하는 공간 ex) 칠판

1. 변수 : 값을 가진 공간의 주소를 가진 이름표
- 변수명 = 값  (=은 대입연산자)
- 변수명 명명 규칙
    1. 영문(대문자), 숫자, $ , __ 로 구성
    2. 숫자는 앞에 x
    3. 공백은 포함 x
    4. 예약이 x (if, for ,true)
1. 변수명 권장 사항
- 의미있는 단어
- 여러 단어 구성은 __로 구분한다
1. 자료 (연산에 사용되는 값)

자료형 (자료의 종류)

1. 숫자형 
- 정수형 - int
- 실수형 - float
1. 문자형 (’  ‘) , (”  “ )
2. 논리형 : True , False

GC(가비지 콜렉터) ex) 청소부

- 메모리를 지켜보며 안쓰는 공간의 쓰레기를 제거함.

# 오늘 배운 점
- python의 문법에 대해서 알 수 있었다.
- python이 버전을 다 여러가지로 다뤄야되는 걸 알았다.
- 변수가 쉽게 말해서 이름표라는 것을 알았다.
- 변수명은 의미있는 단어로만 해야하는 걸 알게 되었다.
  
