# LLM
>날짜: 2026.09.21
---
## Runnable(인터페이스 구현체)
**동기식**

- invoke
- stream : 생성된 토큰마다 출력(chunk)
- batch

**비동기**

- a invoke
- a stream
- a batch(..)

## RunnableLamda => Runnable 객체로 래핑
- 앞선 체인이 Runnable이면 뒤에 있는 함수는 자동으로 Runnable이 된다.